{
  "scripts": {
    "back": "(cd ./back && npm run start)",
    "peerkey": "npm-run-all -p back start",
    "start": "(cd ./front && npm run start)"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
